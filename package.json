{
    "name": "aws-typescript",
    "devDependencies": {
        "@types/node": "^8.0.0",
        "eslint": "^6.7.2",
        "eslint-config-strongloop": "^2.1.0"
    },
    "dependencies": {
        "@pulumi/aws": "^1.0.0",
        "@pulumi/awsx": "^0.18.10",
        "@pulumi/pulumi": "^1.0.0",
        "prettier": "^1.19.1"
    },
    "scripts": {
        "format": "prettier --write **/*.ts",
        "pretest": "eslint --ignore-path .gitignore ."
    }
}
